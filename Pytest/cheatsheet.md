# Pytest – Cheat Sheet

pytest                # esegue tutti i test
pytest -v             # verbose
pytest file.py        # file specifico
pytest -k keyword     # filtra per nome
pytest -m marker      # esegue marker
pytest --maxfail=1    # stop al primo errore
pytest -n 4           # parallelo
